doctype html
html
  head
    title Enter Amount
    script(src='https://cdn.tailwindcss.com')
  body(class='bg-gray-100 flex items-center justify-center h-screen')
    div(class='bg-white p-6 rounded-lg shadow-lg')
      h1(class='text-xl font-bold mb-4') Enter Payment Amount
      p(class='mb-2') Your current balance: ₹#{balance} <!-- Display the balance here -->
      form(action='/process-payment', method='POST', class='space-y-4')
        label(for='amount', class='block text-gray-700') Amount:
        input(type='number', name='amount', required, min='1', class='border rounded p-2 w-full')
        button(type='submit', class='bg-blue-500 text-white p-2 rounded w-full') Pay Now
      if error
        p(class='text-red-500 mt-2')= error