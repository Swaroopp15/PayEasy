doctype html
html
  head
    title OTP Verification
    script(src='https://cdn.tailwindcss.com')
  body(class='bg-gray-100 flex items-center justify-center h-screen')
    div(class='bg-white p-6 rounded-lg shadow-lg')
      h1(class='text-xl font-bold mb-4') Enter OTP Sent to Your Email
      form(action='/verify-auth', method='POST', class='space-y-4')
        input(type='text', name='authInput', required, placeholder='Enter OTP', class='border rounded p-2 w-full')
        input(type='hidden', name='method', value='otp')
        input(type='hidden', name='email', value=email)
        button(type='submit', class='bg-blue-500 text-white p-2 rounded w-full') Verify OTP
      if error
        p(class='text-red-500 mt-2')= error